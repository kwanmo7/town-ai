# API 설계

## 목차
- [공통 규칙](#공통-규칙)
- [공통 HTTP Status](#공통-http-status)
- [공통 오류 코드](#공통-오류-코드)
- [Area API](#area-api)
- [Visit API](#visit-api)
- [Visit Draft API](#visit-draft-api)
- [Report API](#report-api)
- [Statistics API](#statistics-api)

## 공통 규칙
- Base URL: `/api`
- 요청 및 응답 형식: `application/json`
- 날짜 형식: `yyyy-MM-dd`
- 날짜・시간 형식: ISO8601
- ID 타입: `BIGINT`
- 점수 범위: `0` 이상 `10` 이하
- 점수 타입: 정수
- 성공 응답은 공통 Wrapper 없이 리소스를 직접 반환
- 오류 응답은 공통 형식 사용

## 공통 HTTP Status
| HTTP Status | 사용 기준|
|----|----|
| `200 OK` | 조회 및 수정 성공 |
| `201 Created` | 리소스 생성 성공 |
| `204 No Content` | 삭제 성공 |
| `400 Bad Request` | 요청 형식 오류 또는 Validation 실패 |
| `404 Not Found` | 요청한 리소스가 존재하지 않음 |
| `405 Method Not Allowed` | 지원하지 않는 HTTP Method 요청 |
| `409 Conflict` | 리소스 상태 또는 데이터 충돌 |
| `500 Internal Server Error` | 서버 내부 오류 |
| `502 Bad Gateway` | 외부 API 호출 실패 |
| `503 Service Unavailable` | 외부 서비스 또는 저장소 일시 사용 불가 |

## 오류 응답

```json
{
  "timestamp": "2026-07-12T23:30:00+09:00",
  "status":400,
  "code": "VALIDATION_FAILED",
  "message": "요청 값이 올바르지 않습니다.",
  "path": "/api/visits",
  "errors":[
    {
      "field": "atmosphereScore",
      "reason": "0이상 10이하의 값이어야 합니다."
    }
  ]
}
```
- `errors`는 필드 단위 오류가 있는 경우에만 포함
- `code`는 Backend에서 정의한 오류 코드 사용

## 공통 오류 코드

| HTTP Status | code | 사용 기준 |
|----|----|----|
| `400` | `MALFORMED_REQUEST` | JSON 문법 오류, 타입 불일치 등 요청 본문을 읽을 수 없음 |
| `400` | `VALIDATION_FAILED` | 필수 값 누락, 길이·범위·날짜 형식 등 필드 검증 실패 |
| `400` | `INVALID_DATE_RANGE` | 조회 시작일이 종료일보다 늦음 |
| `400` | `INVALID_REPORT_TARGETS` | Report Type에 맞지 않는 `areaIds` 개수, 누락, 중복 또는 불필요한 전달 |
| `400` | `UNSUPPORTED_REPORT_TYPE` | 지원하지 않는 Report Type 요청 |
| `404` | `AREA_NOT_FOUND` | Area가 존재하지 않거나 Soft Delete됨 |
| `404` | `VISIT_NOT_FOUND` | Visit이 존재하지 않음 |
| `404` | `REPORT_NOT_FOUND` | Report가 존재하지 않음 |
| `405` | `METHOD_NOT_ALLOWED` | 해당 경로에서 지원하지 않는 HTTP Method 요청 |
| `409` | `AREA_ALREADY_EXISTS` | `(prefecture, city, name)`이 동일한 Area가 이미 존재 |
| `500` | `INTERNAL_SERVER_ERROR` | 별도 처리되지 않은 서버 내부 오류 |
| `502` | `OPENAI_API_ERROR` | OpenAI API 호출 또는 응답 처리 실패 |
| `503` | `STORAGE_ERROR` | Cloud Storage 저장·조회·삭제 실패 |

- Soft Delete된 Area는 외부에서 존재하지 않는 리소스와 동일하게 `AREA_NOT_FOUND`로 처리한다.
- 오류 `message`는 사용자에게 표시할 수 있는 문장으로 작성하고, 내부 예외 메시지나 민감한 정보를 노출하지 않는다.
- 필드 검증 오류가 여러 개이면 `errors` 배열에 가능한 오류를 함께 반환한다.


## Area API

### 필드 검증 규칙

| 필드 | 필수 | 규칙 |
|----|----|----|
| `name` | 필수 | 문자열, 최대 25자 |
| `prefecture` | 필수 | 문자열, 최대 20자 |
| `city` | 필수 | 문자열, 최대 20자 |
| `station` | 선택 | 문자열, 최대 50자 |

- `prefecture`, `city`, `name`의 조합은 중복될 수 없다.
- 등록 또는 수정 결과 동일한 조합의 다른 Area가 존재하면 `409 Conflict`와 `AREA_ALREADY_EXISTS`를 반환한다.
- 중복 여부를 판단할 때 `station`은 사용하지 않는다. 하나의 지역에 여러 역이 연결될 수 있기 때문이다.

### 지역 등록

```http
POST /api/areas
```

Request
```json
{
  "name": "센터미나미",
  "prefecture": "가나가와현",
  "city": "요코하마시 츠즈키구",
  "station": "센터미나미역"
}
```

Response
```http
201 Created
Location: /api/areas/1
```
```json
{
  "id": 1,
  "name": "센터미나미",
  "prefecture": "가나가와현",
  "city": "요코하마시 츠즈키구",
  "station": "센터미나미역",
  "createdAt": "2026-07-11T23:30:00+09:00",
  "updatedAt": "2026-07-11T23:30:00+09:00"
}
```

### 지역 목록 조회
```http
GET /api/areas
```

Response
```json
[
  {
    "id": 1,
    "name": "센터미나미",
    "prefecture": "가나가와현",
    "city": "요코하마시 츠즈키구",
    "station": "센터미나미역"
  }
]
```

### 지역 상세 조회
```http
GET /api/areas/{areaId}
```

- 존재하지 않거나 삭제된 Area는 `404 Not Found`를 반환한다.
- 단건 조회이므로 성공 시 객체를 반환하며 빈 배열은 반환하지 않는다.

Response
```json
{
  "id": 1,
  "name": "센터미나미",
  "prefecture": "가나가와현",
  "city": "요코하마시 츠즈키구",
  "station": "센터미나미역",
  "createdAt": "2026-07-11T23:30:00+09:00",
  "updatedAt": "2026-07-11T23:30:00+09:00"
}
```

### 지역 수정
```http
PUT /api/areas/{areaId}
```

Request
```json
{
  "name": "센터미나미",
  "prefecture": "가나가와현",
  "city": "요코하마시 츠즈키구",
  "station": "센터미나미역"
}
```

Response
```http
200 OK
```

```json
{
  "id": 1,
  "name": "센터미나미",
  "prefecture": "가나가와현",
  "city": "요코하마시 츠즈키구",
  "station": "센터미나미역",
  "createdAt": "2026-07-11T23:30:00+09:00",
  "updatedAt": "2026-07-12T00:15:00+09:00"
}
```


### 지역 삭제
```http
DELETE /api/areas/{areaId}
```

```http
204 No Content
```

- V1에서는 Area를 물리 삭제하지 않고 Soft Delete한다.
- Soft Delete에는 `deleted_at TIMESTAMP NULL` 컬럼을 사용한다.
- 삭제된 Area는 지역 목록 및 상세 조회에서 노출하지 않는다.
- 삭제된 Area에는 새로운 Visit을 등록하거나 Report 생성 대상으로 지정할 수 없다.
- 기존 Visit은 원본 데이터 보존을 위해 유지한다.
- 삭제되지 않은 Area를 삭제하면 `204 No Content`를 반환한다.
- 이미 삭제된 Area를 다시 삭제하거나 조회 또는 수정하면 `404 Not Found`를 반환한다.
- 복구 API와 보존 기간 이후 Hard Delete 정책은 V2에서 검토한다.


## Visit API

### 필드 검증 규칙

| 필드 | 필수 | 규칙 |
|----|----|----|
| `areaId` | 필수 | 존재하며 삭제되지 않은 Area의 ID |
| `visitDate` | 필수 | `yyyy-MM-dd` 형식 |
| `atmosphereScore` | 필수 | 정수, 0 이상 10 이하 |
| `infraScore` | 필수 | 정수, 0 이상 10 이하 |
| `cleanScore` | 필수 | 정수, 0 이상 10 이하 |
| `sizeScore` | 필수 | 정수, 0 이상 10 이하 |
| `accessScore` | 필수 | 정수, 0 이상 10 이하 |
| `memo` | 선택 | 문자열 |

### 방문 평가 등록
```http
POST /api/visits
```

Request
```json
{
  "id": 1,
  "area": {
    "id": 1,
    "name": "센터미나미"
  },
  "visitDate": "2026-07-12",
  "atmosphereScore": 9,
  "infraScore": 9,
  "cleanScore": 10,
  "sizeScore": 8,
  "accessScore": 7,
  "memo": "길이 넓고 역 주변 생활 인프라가 좋았다."
}
```

Response
```http
201 Created
Location: /api/visits/1
```
```json
{
  "id": 1,
  "area": {
    "id": 1,
    "name": "센터미나미"
  },
  "visitDate": "2026-07-12",
  "atmosphereScore": 9,
  "infraScore": 9,
  "cleanScore": 10,
  "sizeScore": 8,
  "accessScore": 7,
  "memo": "길이 넓고 역 주변 생활 인프라가 좋았다.",
  "createdAt": "2026-07-12T23:30:00+09:00",
  "updatedAt": "2026-07-12T23:30:00+09:00"
}
```

### 방문 평가 목록 조회
```http
GET /api/visits
```

선택 Query Parameter
```http
GET /api/visits?areaId=1
GET /api/visits?from=2026-07-01&to=2026-07-31
GET /api/visits?areaId=1&from=2026-07-01&to=2026-07-31
```

조회 규칙
- `from`과 `to`는 해당 날짜를 포함한다.
- `from`이 `to`보다 늦으면 `400 Bad Request`를 반환한다.
- Visit 목록 필터에서 존재하지 않거나 삭제된 `areaId`를 지정하면 `200 OK`와 빈 배열을 반환한다. 이 규칙은 Area 단건 조회에는 적용하지 않는다.
- 기본 정렬은 `visitDate DESC`, 동일한 방문일은 `id DESC`이다.

Response
```json
[
  {
    "id": 1,
    "area": {
      "id": 1,
      "name": "센터미나미"
    },
    "visitDate": "2026-07-12",
    "atmosphereScore": 9,
    "infraScore": 9,
    "cleanScore": 10,
    "sizeScore": 8,
    "accessScore": 7
  }
]
```

### 방문 평가 상세 조회
```http
GET /api/visits/{visitId}
```

Response
```json
{
  "id": 1,
  "area": {
    "id": 1,
    "name": "센터미나미",
    "prefecture": "가나가와현",
    "city": "요코하마시 츠즈키구",
    "station": "센터미나미역"
  },
  "visitDate": "2026-07-12",
  "atmosphereScore": 9,
  "infraScore": 9,
  "cleanScore": 10,
  "sizeScore": 8,
  "accessScore": 7,
  "memo": "길이 넓고 역 주변 생활 인프라가 좋았다.",
  "createdAt": "2026-07-12T23:30:00+09:00",
  "updatedAt": "2026-07-12T23:30:00+09:00"
}
```

### 방문 평가 수정
```http
PUT /api/visits/{visitId}
```

Request
```json
{
  "areaId": 1,
  "visitDate": "2026-07-12",
  "atmosphereScore": 9,
  "infraScore": 10,
  "cleanScore": 10,
  "sizeScore": 8,
  "accessScore": 7,
  "memo": "재방문 후 생활 인프라 점수를 상향 조정했다.",
  "createdAt": "2026-07-12T23:30:00+09:00",
  "updatedAt": "2026-07-13T00:15:00+09:00"
}
```

Response
```http
200 OK
```
```json
{
  "areaId": 1,
  "visitDate": "2026-07-12",
  "atmosphereScore": 9,
  "infraScore": 10,
  "cleanScore": 10,
  "sizeScore": 8,
  "accessScore": 7,
  "memo": "재방문 후 생활 인프라 점수를 상향 조정했다."
}
```


### 방문 평가 삭제
```http
DELETE /api/visits/{visitId}
```

Response
```http
204 No Content

```

- V1에서 Visit은 Hard Delete한다.
- 존재하지 않는 Visit을 조회, 수정 또는 삭제하면 `404 Not Found`와 `VISIT_NOT_FOUND`를 반환한다.


## Visit Draft API

자연어 입력을 Visit 등록 요청 형태의 초안으로 변환한다. 이 API는 Visit을 저장하지 않으며, 사용자가 결과를 확인한 후 `POST /api/visits`로 등록한다.

- `text`는 필수이며 빈 문자열은 허용하지 않는다.
- OpenAI API 호출 또는 응답 처리에 실패하면 `502 Bad Gateway`와 `OPENAI_API_ERROR`를 반환한다.

### 자연어 방문 평가 파싱
```http
POST /api/visit-drafts
```

Request
```json
{
  "text": "7월 12일 센터미나미에 갔는데 분위기 9점, 접근성 7점이었어."
}
```

Response
```http
200 OK
```
```json
{
  "area": {
    "id": 1,
    "name": "센터미나미"
  },
  "visitDate": "2026-07-12",
  "atmosphereScore": 9,
  "infraScore": null,
  "cleanScore": null,
  "sizeScore": null,
  "accessScore": 7,
  "memo": null,
  "warnings": [
    "infraScore, cleanScore, sizeScore가 입력되지 않았습니다."
  ]
}
```

- 파싱 결과에는 불확실하거나 누락된 필드가 포함될 수 있다.
- 실제 Visit 등록 시에는 모든 필수 필드에 기존 Visit API의 검증 규칙을 적용한다.
- 파싱 결과를 Backend에서 재검증하며, AI가 반환한 Area와 점수를 신뢰하여 바로 저장하지 않는다.


## Report API

### 리포트 유형

| 유형 | 역할 | Prompt Version |
|----|----|----|
| `SUMMARY` | SQL로 산출한 전체 통계와 항목별 Top 5를 중심으로 구성하고 AI는 짧은 Comment만 생성 | `summary-v1` |
| `ALL` | 모든 Area와 Visit 데이터를 기반으로 AI가 전체 지역 상세 분석 리포트 생성 | `all-v1` |
| `AREA` | 하나의 Area와 Visit 데이터를 기반으로 AI가 해당 지역을 상세 분석 | `area-v1` |
| `COMPARE` | 선택한 2개 이상 5개 이하의 Area를 AI가 항목별로 비교 분석 | `compare-v1` |

- 자연어 방문 평가 Parser의 Prompt Version은 `visit-parser-v1`을 사용한다.

### 리포트 생성
```http
POST /api/reports
```

#### SUMMARY
```json
{
  "reportType": "SUMMARY"
}
```

#### ALL
```json
{
  "reportType": "ALL"
}
```

#### AREA
```json
{
  "reportType": "AREA",
  "areaIds": [1]
}
```

#### COMPARE
```json
{
  "reportType": "COMPARE",
  "areaIds": [1, 2, 3]
}
```

#### 검증 규칙
- SUMMARY, ALL: `areaIds` 생략. `null` 또는 빈 배열을 포함하여 필드가 전달되면 `400 Bad Request`
- AREA: `areaIds` 정확히 1개
- COMPARE: `areaIds` 2개 이상 5개 이하
- AREA 또는 COMPARE에서 `areaIds`가 누락되거나 `null` 또는 빈 배열이면 `400 Bad Request`
- 중복 ID는 허용하지 않으며 `400 Bad Request`
- 존재하지 않거나 삭제된 지역 ID가 있으면 `404 Not Found`
- 지원하지 않는 `reportType`이면 `400 Bad Request`

#### 대상 지역 저장 규칙
- Report 생성 시 분석 대상 Area를 `report_area` 연결 테이블에 저장한다.
- AREA는 1개, COMPARE는 요청 순서대로 2개 이상 5개 이하의 관계를 저장한다.
- ALL은 생성 당시 분석에 포함된 모든 Area 관계를 저장한다.
- SUMMARY는 SQL 전체 통계를 기반으로 하므로 Area 관계를 저장하지 않는다.
- `displayOrder`는 AREA와 COMPARE 요청의 지역 순서 및 ALL 생성 당시의 정렬 순서를 보존한다.

#### 생성 방식
- V1에서는 동기 방식으로 생성한다.
- OpenAI 호출, Markdown 생성, Cloud Storage 저장 및 Report 메타데이터 저장이 모두 완료된 경우에만 `201 Created`를 반환한다.
- OpenAI API 호출 실패 시 `502 Bad Gateway`를 반환한다.
- Cloud Storage 저장 실패 시 `503 Service Unavailable`을 반환한다.
- 생성 과정이 실패하면 Report 메타데이터를 저장하지 않는다.

Response
```http
201 Created
Location: /api/reports/10
```

```json
{
  "id": 10,
  "reportType": "COMPARE",
  "model": "gpt-5.5-mini",
  "promptVersion": "compare-v1",
  "createdAt": "2026-07-12T23:40:00+09:00"
}
```

### 리포트 목록 조회
```http
GET /api/reports
```

선택 Query Parameter
```http
GET /api/reports?reportType=AREA
```

- 지원하지 않는 `reportType`을 전달하면 `400 Bad Request`와 `UNSUPPORTED_REPORT_TYPE`을 반환한다.

Response
```json
[
  {
    "id": 10,
    "reportType": "COMPARE",
    "model": "gpt-5.5-mini",
    "promptVersion": "compare-v1",
    "createdAt": "2026-07-12T23:40:00+09:00"
  }
]
```

- 기본 정렬은 `createdAt DESC`, 생성 시각이 같으면 `id DESC`이다.
- `storagePath`는 서버 내부 저장 경로이므로 API 응답에 노출하지 않는다.

### 리포트 상세 조회
```http
GET /api/reports/{reportId}
```

- 존재하지 않는 Report는 `404 Not Found`와 `REPORT_NOT_FOUND`를 반환한다.

Response
```json
{
  "id": 10,
  "reportType": "COMPARE",
  "areaIds": [1, 2, 3],
  "model": "gpt-5.5-mini",
  "promptVersion": "compare-v1",
  "createdAt": "2026-07-12T23:40:00+09:00"
}
```

### 리포트 내용 조회
```http
GET /api/reports/{reportId}/content
```

- 존재하지 않는 Report는 `404 Not Found`와 `REPORT_NOT_FOUND`를 반환한다.
- Cloud Storage에서 내용을 조회하지 못하면 `503 Service Unavailable`과 `STORAGE_ERROR`를 반환한다.

Response
```http
200 OK
Content-Type: text/markdown; charset=UTF-8
```

```Markdown
# 지역 비교 리포트
...
```

### 리포트 다운로드
```http
GET /api/reports/{reportId}/download
```

- 존재하지 않는 Report는 `404 Not Found`와 `REPORT_NOT_FOUND`를 반환한다.
- Cloud Storage에서 파일을 조회하지 못하면 `503 Service Unavailable`과 `STORAGE_ERROR`를 반환한다.

Response
```http
200 OK
Content-Type: text/markdown; charset=UTF-8
Content-Disposition: attachment; filename="compare-report-10.md"
```

### 리포트 삭제
```http
DELETE /api/reports/{reportId}
```

Response
```http
204 No Content
```

- Report 삭제는 Cloud Storage 객체를 먼저 삭제한 후 DB 데이터를 삭제한다.
- 존재하지 않는 Report를 삭제하면 `404 Not Found`와 `REPORT_NOT_FOUND`를 반환한다.
- Cloud Storage 객체가 이미 존재하지 않으면 삭제 성공으로 간주하고 DB 삭제를 계속한다.
- Cloud Storage 삭제에 실패하면 DB 데이터는 유지하고 `503 Service Unavailable`을 반환한다.
- Cloud Storage 삭제 성공 후 `report_area` 관계 데이터와 Report 데이터를 함께 DB 트랜잭션으로 삭제한다.
- DB와 Cloud Storage는 하나의 트랜잭션으로 묶을 수 없으므로 부분 실패 가능성을 로그로 기록하고 운영 복구 대상으로 관리한다.

## Statistics API

### 집계 규칙

- 전체 평균은 삭제되지 않은 Area에 속한 모든 Visit을 같은 비중으로 계산한다.
- 지역별 평균은 해당 Area에 속한 모든 Visit을 같은 비중으로 계산한다.
- Top 5는 각 Area의 지역별 평균을 기준으로 산정한다.
- 평균은 소수점 둘째 자리에서 반올림하여 소수점 첫째 자리까지 반환한다.
- Top 5 대상 데이터가 없으면 빈 배열을 반환한다.
- Top 5는 점수 내림차순으로 정렬하고, 점수가 같으면 `areaId` 오름차순으로 정렬한다.
- Soft Delete된 Area와 해당 Area의 Visit은 전체 통계 및 Top 5에서 제외한다.

### 전체 통계 조회
```http
GET /api/statistics
```

Response
```json
{
  "areaCount": 6,
  "visitCount": 12,
  "averageScores": {
    "atmosphere": 8.4,
    "infra": 8.1,
    "clean": 8.8,
    "size": 8.3,
    "access": 7.2
  },
  "top5": {
    "atmosphere": [
      {
        "areaId": 1,
        "areaName": "센터미나미",
        "score": 9.4
      }
    ],
    "infra": [],
    "clean": [],
    "size": [],
    "access": []
  }
}
```

### 지역별 통계 조회
```http
GET /api/areas/{areaId}/statistics
```

- 존재하지 않거나 삭제된 Area는 `404 Not Found`를 반환한다.

Response
```json
{
  "areaId": 1,
  "areaName": "센터미나미",
  "visitCount": 3,
  "averageScores": {
    "atmosphere": 8.7,
    "infra": 9.0,
    "clean": 8.3,
    "size": 8.0,
    "access": 7.7
  }
}
```

